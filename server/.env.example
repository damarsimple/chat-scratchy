# PostgreSQL connection string (Docker: port 5433 by convention)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DBNAME?schema=public"

# Secret for signing teacher session JWTs (httpOnly cookie). Change in production.
JWT_SECRET="change-me"
