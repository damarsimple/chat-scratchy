add button => debug on chat container
d

debug, discuss, and something else? 

mode 1 = learning mode
mode 2 = task mode (maybe add objectives here e.g move sprite 5 steps.)

actively suggest debugging when user is stuck for a while, or when the code is not working as expected. 

even suggesting get rest or take a break when user is stuck for a while.

not passive but active